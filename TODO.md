# TODO: Fix Rust Compilation Errors

## Tasks
- [x] Fix borrow checker errors in src/app.rs by changing draw_ui to take &self instead of &mut self
- [x] Fix unused variable warning in src/ui/views/repo_view.rs by prefixing repo with underscore
- [x] Remove unused imports in src/app.rs by moving to top and removing local uses
- [x] Test compilation after fixes
